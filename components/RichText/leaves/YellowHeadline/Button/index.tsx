import React from 'react';
import { LeafButton } from 'payload/components/rich-text';

const Button: React.FC = () => (
  <LeafButton format="yellow-headline">
    Yellow Headline
  </LeafButton>
);

export default Button;
