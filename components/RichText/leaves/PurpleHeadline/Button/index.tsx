import React from 'react';
import { LeafButton } from 'payload/components/rich-text';

const Button: React.FC = () => (
  <LeafButton format="purple-headline">
    Purple Headline
  </LeafButton>
);

export default Button;
