import React from 'react';
import { LeafButton } from 'payload/components/rich-text';

const Button: React.FC = () => (
  <LeafButton format="white-headline">
    White Headline
  </LeafButton>
);

export default Button;
