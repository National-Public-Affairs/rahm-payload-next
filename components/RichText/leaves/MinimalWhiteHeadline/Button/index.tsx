import React from 'react';
import { LeafButton } from 'payload/components/rich-text';

const Button: React.FC = () => (
  <LeafButton format="minimal-white-headline">
    White Headline
  </LeafButton>
);

export default Button;
